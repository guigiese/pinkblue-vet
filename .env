# ── PinkBlue Vet — Desenvolvimento Local ────────────────────────────────────
# Este arquivo NUNCA deve ser commitado. Está no .gitignore.
# Espelha as variáveis de produção do Railway.

# ── Banco de dados (PostgreSQL local — espelha prod) ─────────────────────────
# PostgreSQL 16 local (winget). Banco criado em 2026-04-08.
# Cria o banco se necessário: createdb -U pinkblue pinkblue_dev
DATABASE_URL=postgresql://pinkblue:change-me-dev@localhost:5432/pinkblue_dev

# ── Autenticação da plataforma ────────────────────────────────────────────────
PB_MASTER_EMAIL=guigiese@gmail.com
PB_MASTER_PASSWORD=
PB_MASTER_FORCE_CHANGE=false
PB_AUTH_ENABLED=true
PB_SESSION_TTL_DAYS=14
PB_DEV_DATABASE_URL=postgresql://pinkblue:change-me-dev@localhost:5432/pinkblue_dev
